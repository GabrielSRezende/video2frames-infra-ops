apiVersion: 1

providers:
  - name: video2frames
    orgId: 1
    folder: video2frames
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
